# mylibrary/string_utils/validator.py

def is_palindrome(text: str) -> bool:
    """
    Hint: Word that reads the same backward or forward

    Example: 
    Input: "ACCA" 
    Reversed: "ACCA"

    Since forward or backward reads is same, hence "ACCA" is palindrome.
    return True otherwise False.
    """
    return None 

